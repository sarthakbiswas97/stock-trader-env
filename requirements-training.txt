# Training dependencies for RunPod
# Install with: pip install -r requirements-training.txt

# Base project deps
-r requirements.txt

# Core ML
gymnasium
mlflow
datasets
huggingface_hub

# Unsloth (must install typing_extensions first: pip install -q --upgrade typing_extensions)
unsloth
unsloth_zoo

# TRL + GRPO deps (install all optional deps to avoid import chain failures)
trl[all]

# Stable Baselines (PPO baseline)
stable-baselines3
