# Training dependencies for RunPod
# Install with: pip install --upgrade typing_extensions && pip install -r requirements-training.txt && pip install -e .

# Base project deps
-r requirements.txt

# Must be upgraded before unsloth/trl
typing_extensions>=4.12

# Core ML
gymnasium
mlflow
datasets
huggingface_hub

# Unsloth
unsloth
unsloth_zoo

# TRL + GRPO (all optional deps to avoid import chain failures)
trl[all]

# Stable Baselines (PPO baseline)
stable-baselines3
